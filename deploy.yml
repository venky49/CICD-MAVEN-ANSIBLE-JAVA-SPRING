---
- hosts: web
  sudo: yes
  gather_facts: no
  tasks:
   - name: stat foo
     copy:
      src: /tmp/helloworld.war
      dest: /usr/share/tomcat/webapps/

   - name: restart tomcat service
     service:
       name: tomcat
       state: restarted

