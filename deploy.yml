---
- hosts: web
  gather_facts: yes
  tasks:
  
   - name: checking ping
     ping:
   - name: copy the build artifcats into tomcat location
     copy:
      src: /tmp/helloworld.war
      dest: /usr/share/tomcat/webapps/
      owner: root
      group: root
      mode: 0644  
   - name: restart tomcat service
     service:
       name: tomcat
       state: started
       
       
      
   
     

